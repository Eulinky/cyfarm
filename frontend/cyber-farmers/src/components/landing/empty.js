import React from 'react'
import { func, bool } from 'prop-types'

const LandingPage = ({ login, displayError }) => (
    <div></div>
)
  
LandingPage.propTypes = {
    
}

export default ProjectsPage
